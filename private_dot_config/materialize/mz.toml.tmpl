profile = "staging"

[profiles]

[profiles.staging]
app-password = "{{ include "private_dot_config/materialize/staging_credential.age" | decrypt | trim }}"
region = "aws/us-east-1"
admin-endpoint = "https://admin.staging.cloud.materialize.com/"
cloud-endpoint = "https://api.staging.cloud.materialize.com/"

[profiles.prod]
app-password = "{{ include "private_dot_config/materialize/prod_credential.age" | decrypt | trim }}"
region = "aws/us-east-1"
