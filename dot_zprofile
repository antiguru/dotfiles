# User specific environment and startup programs
# fortune

# export CLASSPATH=$(find /usr/share/java ~/lib -name "*.jar" -type f -printf "%p:")
# export CLASSPATH=$(find ~/lib -name "*.jar" \( -type f -o -type l \) -printf "%p:")
#export JAVA_HOME=/usr/lib/j2sdk1.6-ibm

export GDK_NATIVE_WINDOWS=true
export QT_AUTO_SCREEN_SCALE_FACTOR=0
export QT_SCALE_FACTOR=2

#export PROMPT='%n@%m:%~%#' # default prompt
#export RPROMPT='%?' # prompt for right side of screen

typeset -U path
path=(~/.local/bin ~/.cargo/bin/ $path[@])

# Added by Toolbox App
export PATH="$PATH:/home/moritz/.local/share/JetBrains/Toolbox/scripts"
